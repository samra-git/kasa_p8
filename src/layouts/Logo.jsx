import React from 'react';
import logo from '../assets/LOGO.png'

const Logo = () => {
    return (
        <div>
            <img src={logo} alt="logo kasa" />
        </div>
    );
};

export default Logo;